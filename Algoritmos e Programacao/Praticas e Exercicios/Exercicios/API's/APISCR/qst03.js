import { question } from "readline-sync";

function main(){
    let quantidade = Number(question("Digite a quantidade de cidades: "))
    let renda = Number(question("Digite a renda da cidade"))
    let contador = 0

    while(contador < quantidade && renda > 0){
        
    }
}